schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.content.write-artifact-through-treedx-proxy.154
journeyIndex: 154
type: agent
subtype: content
journey: Write Artifact Through Treedx Proxy
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Verify agent runtime behavior for Write Artifact Through Treedx Proxy."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.content.treedx-write-artifact
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.content.treedx-write-artifact
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.content.treedx-write-artifact
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Write Artifact Through Treedx Proxy."

