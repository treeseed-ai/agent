schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.runtime.report-runtime-readiness.162
journeyIndex: 162
type: agent
subtype: runtime
journey: Report Capacity Provider Runtime Readiness
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "The canonical provider lifecycle reports class-specific readiness and permits agent and platform-operation providers to run concurrently without sharing identity, storage, checkouts, or credentials."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.runtime.report-readiness
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.runtime.report-readiness
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.runtime.report-readiness
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Readiness is owned by the provider lifecycle and doctor path; the legacy services runtime-readiness command was removed."
