schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.provider.register-provider-capabilities.142
journeyIndex: 142
type: capacity
subtype: provider
journey: Register Provider Capabilities
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Verify agent runtime behavior for Register Provider Capabilities."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.provider.register-capabilities
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.provider.register-capabilities
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.provider.register-capabilities
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Register Provider Capabilities."

