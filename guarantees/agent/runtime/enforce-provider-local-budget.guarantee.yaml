schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.runtime.enforce-provider-local-budget.161
journeyIndex: 161
type: agent
subtype: runtime
journey: Enforce Provider Local Budget
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Agent runtime keeps the Enforce Provider Local Budget contract verifiable through active local evidence."
status: active
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.runtime.enforce-provider-local-budget
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.runtime.enforce-provider-local-budget
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.runtime.enforce-provider-local-budget
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Enforce Provider Local Budget."

