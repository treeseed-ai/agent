schemaVersion: treeseed.guarantee/v1
id: guarantee.capacity.provider.heartbeat-provider-runtime.141
journeyIndex: 141
type: capacity
subtype: provider
journey: Heartbeat Provider Runtime
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Verify agent runtime behavior for Heartbeat Provider Runtime."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.provider.heartbeat.once
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.provider.heartbeat.once
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.provider.heartbeat.once
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Heartbeat Provider Runtime."

