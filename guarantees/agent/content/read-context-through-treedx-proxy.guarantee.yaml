schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.content.read-context-through-treedx-proxy.153
journeyIndex: 153
type: agent
subtype: content
journey: Read Context Through Treedx Proxy
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Agent runtime keeps the Read Context Through Treedx Proxy contract verifiable through active local evidence."
status: active
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.content.treedx-read-context
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.content.treedx-read-context
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.content.treedx-read-context
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Read Context Through Treedx Proxy."

