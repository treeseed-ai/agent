schemaVersion: treeseed.guarantee/v1
id: guarantee.workday.agent.produce-workday-summary.165
journeyIndex: 165
type: workday
subtype: agent
journey: Produce Workday Summary
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Agent runtime keeps the Produce Workday Summary contract verifiable through active local evidence."
status: active
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.workday.produce-summary
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.workday.produce-summary
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.workday.produce-summary
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Produce Workday Summary."

