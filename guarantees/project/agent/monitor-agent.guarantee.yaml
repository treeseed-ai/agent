schemaVersion: treeseed.guarantee/v1
id: guarantee.project.agent.monitor-agent.060
journeyIndex: 60
type: project
subtype: agent
journey: Monitor Agent
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Verify agent runtime executions, modes, handlers, assignments, outputs, diagnostics, token counts, errors, and forensic traceability."
status: planned
dependencies:
  journeys:
    - 57
  guarantees:
    - guarantee.project.agent.create-agent.057
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.trace.inspect-execution
content:
  required: true
  verifierRefs:
    - agent.content.treedx-read-context
audit:
  required: true
  verifierRefs:
    - agent.trace.correlate-assignment-mode-run-artifacts
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.trace.redact-provider-secrets
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Runtime monitoring belongs to the agent package; Admin UI monitoring is represented by guarantee.project.agent.monitor-agent-admin-ui.140."

