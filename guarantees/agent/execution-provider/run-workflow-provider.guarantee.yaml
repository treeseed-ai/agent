schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.execution-provider.run-workflow-provider.160
journeyIndex: 160
type: agent
subtype: execution-provider
journey: Run Workflow Provider
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Agent runtime keeps the Run Workflow Provider contract verifiable through active local evidence."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.execution-provider.workflow.contract
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.execution-provider.workflow.contract
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.execution-provider.workflow.contract
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Run Workflow Provider."
