schemaVersion: treeseed.guarantee/v1
id: guarantee.agent.runtime.recover-runner-crash.163
journeyIndex: 163
type: agent
subtype: runtime
journey: Recover Runner Crash
ownerPackage: "@treeseed/agent"
surface: agent-runtime
summary: "Verify agent runtime behavior for Recover Runner Crash."
status: planned
dependencies:
  journeys:
    - 60
  guarantees:
    - guarantee.project.agent.monitor-agent.060
actors:
  allowed:
    - project_manager
    - capacity_operator
    - team_owner
  forbidden:
    - anonymous_user
    - unauthorized_user
devices:
  required:
    - desktop_chromium
  optional:
    - mobile_chromium
gates:
  - core
  - release
preconditions:
  fixtures:
    - integrated_treeseed_project
  notes:
    - "Guarantee starts as planned until all verifier refs execute in integrated environments."
scene:
  required: false
api:
  required: true
  verifierRefs:
    - agent.runtime.recover-runner-crash
content:
  required: false
  verifierRefs: []
audit:
  required: true
  verifierRefs:
    - agent.runtime.recover-runner-crash
negativeCases:
  - id: unauthorized-actor-denied
    actor: anonymous_user
    verifierRefs:
      - agent.runtime.recover-runner-crash
evidence:
  required:
    - guarantee_report
    - verifier_evidence
notes:
  - "Agent runtime guarantee for Recover Runner Crash."

